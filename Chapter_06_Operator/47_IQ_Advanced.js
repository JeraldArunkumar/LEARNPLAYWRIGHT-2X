let j=10
console.log(++j + j); // 22 (returns current value of j, then increments j by 1)
console.log(j); // 11 (j is incremented after its value is returned)



let a=15;
console.log(a++ + ++a); // 31 (returns current value of a, then increments a by 1)
console.log(a); // 17 (a is incremented after its value is returned)

// 15+17=32

let c=10;
console.log(++c + ++c); // 22 (returns current value of c, then increments c by 1)
console.log(c); // 12 (c is incremented after its value is returned)

11 +12

let x=5
console.log(x++ + ++x - --x + x-- + ++x); // 11 (returns current value of x, then increments x by 1)
console.log(x); // 5 (x is incremented after its value is returned)

// 5 + 7 - 6 + 6 + 7 = 19