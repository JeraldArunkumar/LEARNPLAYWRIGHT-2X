let user='admin';
let password='admin123';    
let isAccountStatus=true;

    if (user === 'admin' && password === 'admin123' && isAccountStatus) {
        console.log('User is authenticated and has an active account.');
} else {
    console.log('Authentication failed.');
}
