//Here in Js we can store a function and call it later. Below is the example
//we can store either as Const or let...since the function name is not going to change we stored it as constant

//Otherwise called as Function as expressions

const greet= function(name){
return `hello ${name}`
}

let output = greet('kumaaru')
console.log(output)
